# Makefile generated via ChatGPT
CC=gcc
CFLAGS=-Wall
TARGETS=p1a p1b

# Default target
all: $(TARGETS)

# Pattern rule for compiling each target
%: %.c
	$(CC) $(CFLAGS) -o $@ $<

# Clean up compiled files
clean:
	rm -f $(TARGETS)
